#ifndef INPUT_H
#define INPUT_H

#define MAX_INPUT_SIZE 1024

// read_line() – reads a line from stdin, strips the newline, returns a
//               heap-allocated buffer. NULL on EOF.
char *read_line(void);

#endif
